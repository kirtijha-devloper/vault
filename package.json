{
  "name": "secure-vault-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install --legacy-peer-deps && npx prisma generate",
    "postinstall": "cd backend && npm install --legacy-peer-deps && npx prisma generate"
  }
}
